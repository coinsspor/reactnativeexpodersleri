# 📱 Modül 05: Flexbox - Elemanları Düzenleme ve Hizalama

Bu modülde elemanları ekranda istediğin yere koymayı öğreneceksin. Flexbox, React Native'in düzen (layout) sistemidir.

---

## 1. Flexbox Nedir?

Flexbox, elemanları bir kutu içinde düzenlemek için kullanılan bir sistemdir. Şöyle düşün:

Bir kutun var. İçine 3 tane eşya koyacaksın. Bu eşyalar:
- Yan yana mı dursun, alt alta mı?
- Sola mı dayansın, sağa mı, ortada mı?
- Üste mi dayansın, alta mı, ortada mı?
- Ne kadar yer kaplasın?

İşte Flexbox bu soruların cevabını veriyor.

---

## 2. flex: 1 - Tüm Alanı Kapla

En temel Flexbox özelliği: **flex**.

```tsx
import { View, Text, StyleSheet } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      <Text>Merhaba!</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,                    // ← Bu önemli!
    backgroundColor: 'lightblue',
  },
});
```

### flex: 1 Ne Yapar?

`flex: 1` demek: "Mevcut tüm alanı kapla" demek.

Bunu yazmazsan, View sadece içindeki elemanlar kadar yer kaplar. Yazarsan, tüm ekranı kaplar.

**Dene:** `flex: 1` satırını sil ve farkı gör.

---

## 3. flexDirection - Yön Belirleme

Elemanlar hangi yönde dizilsin?

### flexDirection: 'column' (Varsayılan)

Alt alta dizilir (sütun gibi):

```tsx
import { View, Text, StyleSheet } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      <View style={styles.box1}><Text>1</Text></View>
      <View style={styles.box2}><Text>2</Text></View>
      <View style={styles.box3}><Text>3</Text></View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',    // Alt alta (varsayılan)
    padding: 50,
  },
  box1: { width: 50, height: 50, backgroundColor: 'red' },
  box2: { width: 50, height: 50, backgroundColor: 'green' },
  box3: { width: 50, height: 50, backgroundColor: 'blue' },
});
```

Görünüm:
```
┌─────────────────┐
│ ┌───┐           │
│ │ 1 │           │
│ └───┘           │
│ ┌───┐           │
│ │ 2 │           │
│ └───┘           │
│ ┌───┐           │
│ │ 3 │           │
│ └───┘           │
└─────────────────┘
```

### flexDirection: 'row'

Yan yana dizilir (satır gibi):

```tsx
const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',       // Yan yana
    padding: 50,
  },
  // ... box stilleri aynı
});
```

Görünüm:
```
┌─────────────────┐
│ ┌───┐┌───┐┌───┐ │
│ │ 1 ││ 2 ││ 3 │ │
│ └───┘└───┘└───┘ │
│                 │
│                 │
│                 │
└─────────────────┘
```

### Özet

| Değer | Yön | Açıklama |
|-------|-----|----------|
| `column` | ↓ | Yukarıdan aşağıya (varsayılan) |
| `row` | → | Soldan sağa |
| `column-reverse` | ↑ | Aşağıdan yukarıya |
| `row-reverse` | ← | Sağdan sola |

---

## 4. justifyContent - Ana Eksende Hizalama

`justifyContent`, elemanları **ana eksen** boyunca hizalar.

- `flexDirection: 'column'` ise → ana eksen DİKEY (↓)
- `flexDirection: 'row'` ise → ana eksen YATAY (→)

### Örnek: column + justifyContent

```tsx
import { View, StyleSheet } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      <View style={styles.box} />
      <View style={styles.box} />
      <View style={styles.box} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
    justifyContent: 'flex-start',   // ← Bunu değiştirerek dene
    backgroundColor: '#eee',
    padding: 20,
  },
  box: {
    width: 50,
    height: 50,
    backgroundColor: '#3498db',
    marginBottom: 10,
  },
});
```

### justifyContent Değerleri

**flex-start:** Başa hizala (varsayılan)
```
┌─────────┐
│ █ █ █   │  ← Başta
│         │
│         │
└─────────┘
```

**flex-end:** Sona hizala
```
┌─────────┐
│         │
│         │
│ █ █ █   │  ← Sonda
└─────────┘
```

**center:** Ortala
```
┌─────────┐
│         │
│ █ █ █   │  ← Ortada
│         │
└─────────┘
```

**space-between:** Aralara eşit boşluk, ilk ve son kenarda
```
┌─────────┐
│ █       │
│         │
│ █       │
│         │
│ █       │
└─────────┘
```

**space-around:** Her elemanın etrafında eşit boşluk
```
┌─────────┐
│         │
│ █       │
│         │
│ █       │
│         │
│ █       │
│         │
└─────────┘
```

**space-evenly:** Tüm boşluklar eşit
```
┌─────────┐
│         │
│ █       │
│         │
│ █       │
│         │
│ █       │
│         │
└─────────┘
```

---

## 5. alignItems - Çapraz Eksende Hizalama

`alignItems`, elemanları **çapraz eksen** boyunca hizalar.

- `flexDirection: 'column'` ise → çapraz eksen YATAY (→)
- `flexDirection: 'row'` ise → çapraz eksen DİKEY (↓)

### Örnek: column + alignItems

```tsx
const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
    alignItems: 'flex-start',   // ← Bunu değiştirerek dene
    backgroundColor: '#eee',
    padding: 20,
  },
  box: {
    width: 50,
    height: 50,
    backgroundColor: '#e74c3c',
    marginBottom: 10,
  },
});
```

### alignItems Değerleri

**flex-start:** Başa hizala
```
┌─────────────┐
│ █           │  ← Sol tarafta
│ █           │
│ █           │
└─────────────┘
```

**flex-end:** Sona hizala
```
┌─────────────┐
│           █ │  ← Sağ tarafta
│           █ │
│           █ │
└─────────────┘
```

**center:** Ortala
```
┌─────────────┐
│      █      │  ← Ortada
│      █      │
│      █      │
└─────────────┘
```

**stretch:** Esnet (varsayılan, elemanın width'i olmamalı)
```
┌─────────────┐
│ ███████████ │  ← Tüm genişliği kaplar
│ ███████████ │
│ ███████████ │
└─────────────┘
```

---

## 6. Ekranın Tam Ortasına Koyma

En sık sorulan soru: "Bir şeyi ekranın tam ortasına nasıl koyarım?"

```tsx
import { View, Text, StyleSheet } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      <View style={styles.box}>
        <Text style={styles.text}>Ortadayım!</Text>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',    // Dikeyde ortala
    alignItems: 'center',        // Yatayda ortala
    backgroundColor: '#f0f0f0',
  },
  box: {
    backgroundColor: '#3498db',
    padding: 30,
    borderRadius: 10,
  },
  text: {
    color: 'white',
    fontSize: 18,
  },
});
```

### Formül

```tsx
container: {
  flex: 1,               // Tüm ekranı kapla
  justifyContent: 'center',  // Ana eksende ortala
  alignItems: 'center',      // Çapraz eksende ortala
}
```

Bu üçlü kombinasyon her zaman tam ortaya koyar.

---

## 7. flex ile Oran Belirleme

Birden fazla eleman arasında alanı paylaştırmak için `flex` kullanılır.

```tsx
import { View, Text, StyleSheet } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      <View style={styles.header}>
        <Text style={styles.text}>Header (flex: 1)</Text>
      </View>
      <View style={styles.content}>
        <Text style={styles.text}>Content (flex: 3)</Text>
      </View>
      <View style={styles.footer}>
        <Text style={styles.text}>Footer (flex: 1)</Text>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    flex: 1,                    // 1 birim
    backgroundColor: '#3498db',
    justifyContent: 'center',
    alignItems: 'center',
  },
  content: {
    flex: 3,                    // 3 birim
    backgroundColor: '#2ecc71',
    justifyContent: 'center',
    alignItems: 'center',
  },
  footer: {
    flex: 1,                    // 1 birim
    backgroundColor: '#e74c3c',
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    color: 'white',
    fontSize: 18,
  },
});
```

### Nasıl Çalışır?

Toplam: 1 + 3 + 1 = 5 birim

- Header: 1/5 = %20 alan
- Content: 3/5 = %60 alan  
- Footer: 1/5 = %20 alan

Görünüm:
```
┌────────────────┐
│    Header      │  ← %20
├────────────────┤
│                │
│    Content     │  ← %60
│                │
├────────────────┤
│    Footer      │  ← %20
└────────────────┘
```

---

## 8. Yan Yana Elemanlar (Row Layout)

```tsx
import { View, Text, StyleSheet } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      <View style={styles.row}>
        <View style={styles.item}>
          <Text>Sol</Text>
        </View>
        <View style={styles.item}>
          <Text>Orta</Text>
        </View>
        <View style={styles.item}>
          <Text>Sağ</Text>
        </View>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 50,
  },
  row: {
    flexDirection: 'row',           // Yan yana diz
    justifyContent: 'space-between', // Aralarına eşit boşluk
  },
  item: {
    backgroundColor: '#3498db',
    padding: 20,
    borderRadius: 10,
  },
});
```

---

## 9. Pratik Örnek: Basit Layout

Tipik bir uygulama düzeni yapalım:

```tsx
import { View, Text, StyleSheet } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      
      {/* Üst Bar */}
      <View style={styles.header}>
        <Text style={styles.headerText}>📱 Uygulamam</Text>
      </View>

      {/* Ana İçerik */}
      <View style={styles.content}>
        <Text style={styles.title}>Hoş Geldin!</Text>
        <Text style={styles.subtitle}>Bu benim ilk uygulamamız.</Text>
        
        {/* Yan yana kartlar */}
        <View style={styles.cardRow}>
          <View style={styles.card}>
            <Text style={styles.cardEmoji}>📊</Text>
            <Text style={styles.cardText}>İstatistik</Text>
          </View>
          <View style={styles.card}>
            <Text style={styles.cardEmoji}>⚙️</Text>
            <Text style={styles.cardText}>Ayarlar</Text>
          </View>
        </View>
      </View>

      {/* Alt Bar */}
      <View style={styles.footer}>
        <Text style={styles.footerText}>© 2025 Benim Uygulamam</Text>
      </View>

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f5f5f5',
  },
  
  // Header
  header: {
    backgroundColor: '#3498db',
    paddingTop: 50,           // Durum çubuğu için boşluk
    paddingBottom: 15,
    paddingHorizontal: 20,
  },
  headerText: {
    color: 'white',
    fontSize: 20,
    fontWeight: 'bold',
  },

  // Content
  content: {
    flex: 1,                   // Kalan tüm alanı kapla
    padding: 20,
  },
  title: {
    fontSize: 28,
    fontWeight: 'bold',
    color: '#333',
  },
  subtitle: {
    fontSize: 16,
    color: '#666',
    marginTop: 5,
    marginBottom: 30,
  },

  // Kartlar (yan yana)
  cardRow: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  card: {
    flex: 1,                   // Eşit genişlik
    backgroundColor: 'white',
    padding: 25,
    borderRadius: 15,
    alignItems: 'center',
    marginHorizontal: 5,
    // Gölge
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 3,
  },
  cardEmoji: {
    fontSize: 40,
    marginBottom: 10,
  },
  cardText: {
    fontSize: 14,
    color: '#333',
  },

  // Footer
  footer: {
    backgroundColor: '#333',
    padding: 15,
    alignItems: 'center',
  },
  footerText: {
    color: '#999',
    fontSize: 12,
  },
});
```

---

## 10. gap - Elemanlar Arası Boşluk

Elemanlar arasına boşluk koymak için `gap` kullanabilirsin (margin'e gerek kalmadan):

```tsx
const styles = StyleSheet.create({
  row: {
    flexDirection: 'row',
    gap: 10,                    // Elemanlar arası 10 piksel
  },
});
```

```tsx
column: {
  flexDirection: 'column',
  gap: 15,                      // Alt alta elemanlar arası 15 piksel
}
```

---

## 📝 Bu Modülde Öğrendiklerin

| Özellik | Ne Yapar? |
|---------|-----------|
| `flex: 1` | Mevcut tüm alanı kapla |
| `flexDirection` | Elemanların yönü (column/row) |
| `justifyContent` | Ana eksende hizalama |
| `alignItems` | Çapraz eksende hizalama |
| `gap` | Elemanlar arası boşluk |

### Ortaya Koyma Formülü

```tsx
{
  flex: 1,
  justifyContent: 'center',
  alignItems: 'center',
}
```

### Yan Yana Koyma Formülü

```tsx
{
  flexDirection: 'row',
  justifyContent: 'space-between',  // veya 'center', 'space-around'
}
```

---

## ✅ Kontrol Listesi

- [ ] flex: 1'in ne yaptığını anladım
- [ ] flexDirection'ı denedim (column, row)
- [ ] justifyContent değerlerini denedim
- [ ] alignItems değerlerini denedim
- [ ] Bir şeyi ekranın ortasına koyabiliyorum
- [ ] flex ile alan paylaştırabiliyorum
- [ ] Pratik örneği yaptım

---

## 🔗 Sonraki Modül

[Modül 06: Input ve Button - Kullanıcıdan Veri Alma →](../06-input-button/README.md)
