# 📱 Modül 02: İlk Projeyi Oluşturma ve Çalıştırma

Bu modülde ilk React Native projeni oluşturacak ve çalıştıracaksın.

---

## 1. Proje Klasörü Hazırlama

Önce projelerini koyacağın bir klasör oluşturalım.

### Adım 1: Masaüstüne git

VS Code'da terminal aç (Ctrl + `) ve şunu yaz:

```bash
cd Desktop
```

### Adım 2: Projeler klasörü oluştur

```bash
mkdir ReactNativeProjeler
```

### Adım 3: O klasöre gir

```bash
cd ReactNativeProjeler
```

Artık `C:\Users\SeninkiAdi\Desktop\ReactNativeProjeler` klasöründesin.

---

## 2. Yeni Expo Projesi Oluşturma

Şimdi ilk projeni oluşturacaksın. Terminalde şunu yaz:

```bash
npx create-expo-app@latest ilk-uygulamam
```

### Bu Komut Ne Yapıyor?

Parça parça açıklayalım:

- **npx:** npm paketlerini indirmeden çalıştırır
- **create-expo-app:** Expo projesi oluşturan program
- **@latest:** En son sürümü kullan
- **ilk-uygulamam:** Projenin adı (istediğin adı verebilirsin, Türkçe karakter ve boşluk kullanma)

### Bekleme Süresi

Bu komut biraz sürebilir (1-5 dakika). İnternet hızına bağlı. Bekle, kapama.

Şöyle bir şey soracak:
```
✔ Choose a template: › Blank
```
Ok tuşlarıyla "Blank" seç ve Enter'a bas.

Sonunda şunu görürsün:
```
✅ Your project is ready!
```

---

## 3. Proje Klasörüne Girme

Proje oluştu ama henüz o klasörde değilsin. Şunu yaz:

```bash
cd ilk-uygulamam
```

Artık proje klasörünün içindesin.

---

## 4. Projeyi VS Code'da Açma

Şu komutu yaz:

```bash
code .
```

**Ne yapar?** Bulunduğun klasörü yeni bir VS Code penceresinde açar. Nokta (.) "bu klasör" demek.

Eğer çalışmazsa, VS Code'u manuel aç: File → Open Folder → ilk-uygulamam klasörünü seç.

---

## 5. Proje Dosyalarını Tanıyalım

Sol tarafta dosya listesini görüyorsun. Önemli olanları açıklayalım:

```
ilk-uygulamam/
├── app/                  ← Uygulama kodların buraya
│   └── index.tsx         ← Ana sayfa (bunu düzenleyeceksin)
├── assets/               ← Resimler, ikonlar buraya
├── node_modules/         ← İndirilen paketler (dokunma!)
├── app.json              ← Uygulama ayarları
├── package.json          ← Proje bilgileri ve bağımlılıklar
└── tsconfig.json         ← TypeScript ayarları
```

### Önemli Dosyalar

**app/index.tsx:** Ana sayfa. Uygulama açılınca bu dosyadaki kod çalışır. Değişikliklerini burada yapacaksın.

**package.json:** Projenin kimlik kartı gibi. İsmi, versiyonu ve hangi paketleri kullandığı yazıyor.

**node_modules:** npm'in indirdiği paketler. Bu klasör çok büyük olabilir (200MB+). Dokunma, silme.

---

## 6. Projeyi Çalıştırma

### Adım 1: Terminal Aç

VS Code'da Ctrl + ` ile terminal aç.

### Adım 2: Başlat Komutunu Yaz

```bash
npx expo start
```

### Bu Komut Ne Yapıyor?

- Expo geliştirme sunucusunu başlatır
- Kodlarını paketler
- QR kod oluşturur

### Göreceğin Ekran

```
Starting Metro Bundler
█▀▀▀▀▀█ ▄▄▄▄▄ █▀▀▀▀▀█
█ ███ █ █▄▄▄█ █ ███ █
█ ▀▀▀ █ ▀▄▄▄▀ █ ▀▀▀ █    ← QR Kod
▀▀▀▀▀▀▀ ▀ ▀ ▀ ▀▀▀▀▀▀▀

› Metro waiting on exp://192.168.1.x:8081
› Scan the QR code above with Expo Go (Android) or Camera app (iOS)

› Press a │ open Android
› Press w │ open web
› Press r │ reload app
› Press j │ open debugger
› Press ? │ show all commands
```

---

## 7. Telefonda Görme

### Android İçin:
1. Telefonunda Expo Go uygulamasını aç
2. "Scan QR Code" seçeneğine tıkla
3. Terminaldeki QR kodu tara

### iPhone İçin:
1. Kamera uygulamasını aç
2. QR kodu kameraya göster
3. Çıkan bildirime tıkla

### Bilgisayar ve Telefon Aynı WiFi'de Olmalı!

Eğer telefon uygulamayı göremiyorsa, ikisi de aynı WiFi ağına bağlı olmalı.

---

## 8. İlk Değişikliğini Yap

### Adım 1: app/index.tsx Dosyasını Aç

Sol taraftaki dosya listesinden `app` klasörüne tıkla, sonra `index.tsx` dosyasına tıkla.

### Adım 2: Kodu İncele

Şuna benzer bir kod göreceksin:

```tsx
import { Text, View } from 'react-native';

export default function Index() {
  return (
    <View>
      <Text>Welcome!</Text>
    </View>
  );
}
```

### Adım 3: Yazıyı Değiştir

"Welcome!" yazan yeri bul ve değiştir:

```tsx
import { Text, View } from 'react-native';

export default function Index() {
  return (
    <View>
      <Text>Merhaba Dünya! 🎉</Text>
    </View>
  );
}
```

### Adım 4: Kaydet

Ctrl + S ile kaydet.

### Adım 5: Telefona Bak

Telefonunda otomatik olarak "Merhaba Dünya! 🎉" yazısını göreceksin. Sayfa yenilendi!

Bu özelliğe **Hot Reload** (Sıcak Yenileme) deniyor. Kod değiştirince uygulama otomatik güncelleniyor.

---

## 9. Projeyi Durdurma

Terminalde Ctrl + C tuşlarına bas. Sunucu durur.

Tekrar başlatmak için yine `npx expo start` yaz.

---

## 10. Sık Yapılan Hatalar

### Hata: "expo: command not found"
**Çözüm:** Node.js düzgün kurulmamış. Bilgisayarı yeniden başlat veya Node.js'i tekrar kur.

### Hata: Telefon QR kodu okumuyor
**Çözüm:** Bilgisayar ve telefon aynı WiFi'de olmalı.

### Hata: "Unable to resolve module"
**Çözüm:** Terminalde şunu çalıştır:
```bash
npm install
```

### Hata: Metro Bundler açılmıyor
**Çözüm:** 8081 portu başka program tarafından kullanılıyor olabilir. Şunu dene:
```bash
npx expo start --port 8082
```

---

## 📝 Bu Modülde Öğrendiklerin

1. `npx create-expo-app` ile proje oluşturma
2. `cd` ile klasöre girme
3. `npx expo start` ile projeyi çalıştırma
4. QR kod ile telefonda test etme
5. Kod değiştirip Hot Reload görme

---

## ✅ Kontrol Listesi

- [ ] Proje oluşturdum
- [ ] `npx expo start` çalıştırdım
- [ ] Telefonda uygulamayı gördüm
- [ ] Yazıyı değiştirip kaydettim
- [ ] Telefonda değişikliği gördüm

---

## 🔗 Sonraki Modül

[Modül 03: View ve Text - Ekrana Kutu ve Yazı Koyma →](../03-view-text/README.md)
