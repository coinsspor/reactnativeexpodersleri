# 📱 Modül 06: Input ve Button - Kullanıcıdan Veri Alma

Bu modülde kullanıcının yazı yazmasını ve butona tıklamasını öğreneceksin.

---

## 1. State Nedir? (Çok Önemli!)

Butona basmadan önce önemli bir kavram: **State** (Durum).

State, uygulamanın "hatırladığı" bilgidir. Mesela:
- Kullanıcı adı girdi → bunu hatırla
- Butona kaç kez basıldı → bunu hatırla
- Açık/kapalı durumu → bunu hatırla

### Neden Gerekli?

Normal bir değişken React'ta çalışmaz:

```tsx
// ❌ Bu ÇALIŞMAZ
let sayi = 0;

function artir() {
  sayi = sayi + 1;  // Değişir ama ekran güncellenmez!
}
```

React'ta ekranın güncellenmesi için **useState** kullanılmalı.

---

## 2. useState Kullanımı

```tsx
import { useState } from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

export default function Index() {
  // useState kullanımı
  const [sayi, setSayi] = useState(0);
  
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Sayı: {sayi}</Text>
      <Button title="Artır" onPress={() => setSayi(sayi + 1)} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 40,
    marginBottom: 20,
  },
});
```

### Satır Satır Açıklama

```tsx
import { useState } from 'react';
```
`useState` fonksiyonunu React'tan alıyoruz.

```tsx
const [sayi, setSayi] = useState(0);
```
Bu satır çok önemli! Parçalayalım:

- `useState(0)` → Başlangıç değeri 0 olan bir state oluştur
- `sayi` → State'in şu anki değeri (okumak için)
- `setSayi` → State'i değiştiren fonksiyon (yazmak için)

```tsx
<Text style={styles.text}>Sayı: {sayi}</Text>
```
`{sayi}` süslü parantez içinde JavaScript değişkeni yazıyoruz. Ekranda "Sayı: 0" görünür.

```tsx
<Button title="Artır" onPress={() => setSayi(sayi + 1)} />
```
- `Button` → Buton bileşeni
- `title="Artır"` → Butonun üzerindeki yazı
- `onPress` → Butona basıldığında ne olacak
- `() => setSayi(sayi + 1)` → sayi'yi 1 artır

---

## 3. TextInput - Yazı Girişi

Kullanıcının yazı yazması için TextInput kullanılır.

```tsx
import { useState } from 'react';
import { View, Text, TextInput, StyleSheet } from 'react-native';

export default function Index() {
  const [isim, setIsim] = useState('');

  return (
    <View style={styles.container}>
      <Text style={styles.label}>İsminizi girin:</Text>
      
      <TextInput
        style={styles.input}
        value={isim}
        onChangeText={setIsim}
        placeholder="İsim..."
      />

      <Text style={styles.greeting}>
        Merhaba, {isim || 'Misafir'}!
      </Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 50,
  },
  label: {
    fontSize: 18,
    marginBottom: 10,
  },
  input: {
    borderWidth: 1,
    borderColor: '#ddd',
    borderRadius: 8,
    padding: 15,
    fontSize: 16,
    marginBottom: 20,
  },
  greeting: {
    fontSize: 24,
    fontWeight: 'bold',
  },
});
```

### Açıklama

```tsx
const [isim, setIsim] = useState('');
```
Boş string ile başlayan bir state.

```tsx
<TextInput
  style={styles.input}
  value={isim}              // Input'un içindeki değer
  onChangeText={setIsim}    // Her harf yazıldığında state'i güncelle
  placeholder="İsim..."     // Boşken görünen ipucu yazısı
/>
```

```tsx
{isim || 'Misafir'}
```
`||` (veya) operatörü: isim boşsa 'Misafir' yaz.

---

## 4. TextInput Özellikleri

```tsx
<TextInput
  // Temel
  value={deger}
  onChangeText={setDeger}
  placeholder="İpucu yazısı"
  placeholderTextColor="#999"

  // Klavye tipi
  keyboardType="default"        // Normal klavye
  keyboardType="email-address"  // E-posta klavyesi (@, . var)
  keyboardType="phone-pad"      // Telefon tuş takımı
  keyboardType="number-pad"     // Sadece sayılar

  // Diğer
  secureTextEntry={true}        // Şifre girişi (*** gösterir)
  maxLength={50}                // Maksimum karakter sayısı
  autoCapitalize="none"         // Otomatik büyük harf kapalı
  autoCorrect={false}           // Otomatik düzeltme kapalı
  multiline={true}              // Çok satırlı (textarea gibi)
  numberOfLines={4}             // Çok satırlı ise kaç satır
/>
```

---

## 5. Tam Örnek: Giriş Formu

```tsx
import { useState } from 'react';
import { View, Text, TextInput, Button, StyleSheet, Alert } from 'react-native';

export default function Index() {
  const [isim, setIsim] = useState('');
  const [email, setEmail] = useState('');
  const [sifre, setSifre] = useState('');

  const girisYap = () => {
    // Boş alan kontrolü
    if (!isim || !email || !sifre) {
      Alert.alert('Hata', 'Lütfen tüm alanları doldurun!');
      return;
    }

    // Başarılı
    Alert.alert('Başarılı', `Hoş geldin, ${isim}!`);
  };

  return (
    <View style={styles.container}>
      <Text style={styles.title}>📝 Kayıt Formu</Text>

      {/* İsim */}
      <Text style={styles.label}>İsim</Text>
      <TextInput
        style={styles.input}
        value={isim}
        onChangeText={setIsim}
        placeholder="Adınız Soyadınız"
      />

      {/* Email */}
      <Text style={styles.label}>E-posta</Text>
      <TextInput
        style={styles.input}
        value={email}
        onChangeText={setEmail}
        placeholder="ornek@email.com"
        keyboardType="email-address"
        autoCapitalize="none"
      />

      {/* Şifre */}
      <Text style={styles.label}>Şifre</Text>
      <TextInput
        style={styles.input}
        value={sifre}
        onChangeText={setSifre}
        placeholder="******"
        secureTextEntry={true}
      />

      {/* Buton */}
      <View style={styles.buttonContainer}>
        <Button title="Kayıt Ol" onPress={girisYap} />
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 30,
    paddingTop: 80,
    backgroundColor: '#f5f5f5',
  },
  title: {
    fontSize: 28,
    fontWeight: 'bold',
    textAlign: 'center',
    marginBottom: 40,
  },
  label: {
    fontSize: 16,
    fontWeight: '600',
    marginBottom: 8,
    color: '#333',
  },
  input: {
    backgroundColor: 'white',
    borderWidth: 1,
    borderColor: '#ddd',
    borderRadius: 10,
    padding: 15,
    fontSize: 16,
    marginBottom: 20,
  },
  buttonContainer: {
    marginTop: 10,
  },
});
```

### Alert Nedir?

```tsx
Alert.alert('Başlık', 'Mesaj');
```
Telefonun standart uyarı kutusunu gösterir.

---

## 6. TouchableOpacity - Özel Buton

`Button` çok basit görünür. Özelleştirilmiş buton için `TouchableOpacity` kullanılır.

```tsx
import { useState } from 'react';
import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';

export default function Index() {
  const [sayi, setSayi] = useState(0);

  return (
    <View style={styles.container}>
      <Text style={styles.text}>{sayi}</Text>

      <View style={styles.buttonRow}>
        {/* Azalt butonu */}
        <TouchableOpacity
          style={[styles.button, styles.redButton]}
          onPress={() => setSayi(sayi - 1)}
        >
          <Text style={styles.buttonText}>- Azalt</Text>
        </TouchableOpacity>

        {/* Artır butonu */}
        <TouchableOpacity
          style={[styles.button, styles.greenButton]}
          onPress={() => setSayi(sayi + 1)}
        >
          <Text style={styles.buttonText}>+ Artır</Text>
        </TouchableOpacity>
      </View>

      {/* Sıfırla butonu */}
      <TouchableOpacity
        style={styles.resetButton}
        onPress={() => setSayi(0)}
      >
        <Text style={styles.resetText}>Sıfırla</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#f0f0f0',
  },
  text: {
    fontSize: 80,
    fontWeight: 'bold',
    marginBottom: 40,
  },
  buttonRow: {
    flexDirection: 'row',
    gap: 20,
  },
  button: {
    paddingVertical: 15,
    paddingHorizontal: 30,
    borderRadius: 10,
  },
  redButton: {
    backgroundColor: '#e74c3c',
  },
  greenButton: {
    backgroundColor: '#27ae60',
  },
  buttonText: {
    color: 'white',
    fontSize: 18,
    fontWeight: 'bold',
  },
  resetButton: {
    marginTop: 30,
    padding: 15,
  },
  resetText: {
    color: '#666',
    fontSize: 16,
  },
});
```

### TouchableOpacity vs Button

| Özellik | Button | TouchableOpacity |
|---------|--------|------------------|
| Özelleştirme | Sınırlı | Tam özgürlük |
| Stil | Platform'a göre | Kendim belirlerim |
| İçerik | Sadece yazı | Her şey (resim, icon...) |

---

## 7. Birden Fazla State

```tsx
import { useState } from 'react';
import { View, Text, TextInput, TouchableOpacity, FlatList, StyleSheet } from 'react-native';

export default function Index() {
  // Birden fazla state
  const [gorev, setGorev] = useState('');
  const [gorevler, setGorevler] = useState([]);

  const gorevEkle = () => {
    if (!gorev.trim()) return;  // Boşsa ekleme
    
    setGorevler([...gorevler, gorev]);  // Listeye ekle
    setGorev('');  // Input'u temizle
  };

  const gorevSil = (index) => {
    const yeniListe = gorevler.filter((_, i) => i !== index);
    setGorevler(yeniListe);
  };

  return (
    <View style={styles.container}>
      <Text style={styles.title}>📋 Yapılacaklar</Text>

      {/* Input ve Ekle butonu */}
      <View style={styles.inputRow}>
        <TextInput
          style={styles.input}
          value={gorev}
          onChangeText={setGorev}
          placeholder="Yeni görev..."
        />
        <TouchableOpacity style={styles.addButton} onPress={gorevEkle}>
          <Text style={styles.addButtonText}>+</Text>
        </TouchableOpacity>
      </View>

      {/* Görev listesi */}
      {gorevler.length === 0 ? (
        <Text style={styles.empty}>Henüz görev yok</Text>
      ) : (
        gorevler.map((item, index) => (
          <View key={index} style={styles.todoItem}>
            <Text style={styles.todoText}>{item}</Text>
            <TouchableOpacity onPress={() => gorevSil(index)}>
              <Text style={styles.deleteText}>🗑️</Text>
            </TouchableOpacity>
          </View>
        ))
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 30,
    paddingTop: 80,
    backgroundColor: '#f5f5f5',
  },
  title: {
    fontSize: 28,
    fontWeight: 'bold',
    marginBottom: 30,
  },
  inputRow: {
    flexDirection: 'row',
    marginBottom: 20,
  },
  input: {
    flex: 1,
    backgroundColor: 'white',
    borderWidth: 1,
    borderColor: '#ddd',
    borderRadius: 10,
    padding: 15,
    fontSize: 16,
    marginRight: 10,
  },
  addButton: {
    backgroundColor: '#3498db',
    width: 50,
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },
  addButtonText: {
    color: 'white',
    fontSize: 28,
  },
  empty: {
    textAlign: 'center',
    color: '#999',
    marginTop: 50,
  },
  todoItem: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: 'white',
    padding: 15,
    borderRadius: 10,
    marginBottom: 10,
  },
  todoText: {
    fontSize: 16,
  },
  deleteText: {
    fontSize: 20,
  },
});
```

### Yeni Kavramlar

```tsx
[...gorevler, gorev]
```
`...` (spread operatörü) mevcut listeyi açar ve yeni elemanı ekler.

```tsx
gorevler.filter((_, i) => i !== index)
```
Belirli index'teki elemanı silen yeni bir liste oluşturur.

```tsx
gorevler.map((item, index) => (...))
```
Listenin her elemanı için JSX oluşturur.

---

## 📝 Bu Modülde Öğrendiklerin

1. **useState:** Değişken tutma ve güncelleme
2. **TextInput:** Kullanıcıdan yazı alma
3. **Button:** Basit buton
4. **TouchableOpacity:** Özelleştirilebilir buton
5. **Alert:** Uyarı mesajı gösterme
6. **Spread operatörü:** `[...array, newItem]`
7. **map/filter:** Liste işlemleri

---

## ✅ Kontrol Listesi

- [ ] useState'in ne yaptığını anladım
- [ ] TextInput kullanabiliyorum
- [ ] Buton tıklamasını yakalayabiliyorum
- [ ] TouchableOpacity ile özel buton yapabiliyorum
- [ ] Giriş formu örneğini yaptım
- [ ] Yapılacaklar listesi örneğini yaptım

---

## 🔗 Sonraki Modül

[Modül 07: Listeler - Uzun Verileri Gösterme →](../07-listeler/README.md)
