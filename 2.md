# 📱 Modül 03: View ve Text - Ekrana Kutu ve Yazı Koyma

Bu modülde React Native'in en temel iki yapı taşını öğreneceksin: **View** ve **Text**.

---

## 1. React Native Nasıl Çalışır?

Web sitesi yaparken HTML kullanırsın:
```html
<div>
  <p>Merhaba</p>
</div>
```

React Native'de HTML yok. Onun yerine **özel bileşenler** kullanıyoruz:

| HTML | React Native | Ne İşe Yarar? |
|------|--------------|---------------|
| `<div>` | `<View>` | Kutu, kapsayıcı |
| `<p>`, `<span>` | `<Text>` | Yazı gösterme |
| `<img>` | `<Image>` | Resim gösterme |
| `<input>` | `<TextInput>` | Yazı girişi |
| `<button>` | `<Button>` | Buton |

Bu bileşenleri `react-native` paketinden alıyoruz (import ediyoruz).

---

## 2. View Nedir?

**View**, ekranda görünmez bir kutudur. İçine başka şeyler koyarsın.

Şöyle düşün: Bir karton kutu. Kutunun kendisi görünmez ama içine koyduğun şeyler görünür. Kutuyu boyayabilir, kenarlık ekleyebilir, boyutunu ayarlayabilirsin.

### View Kullanımı

```tsx
import { View } from 'react-native';

export default function Index() {
  return (
    <View>
      {/* Buraya içerik gelecek */}
    </View>
  );
}
```

### Açıklama

```tsx
import { View } from 'react-native';
```
**Ne yapar?** `react-native` paketinden `View` bileşenini alır. Bunu yapmadan View kullanamazsın.

```tsx
<View>
</View>
```
**Ne yapar?** Ekrana boş bir kutu koyar. İçi boş olduğu için hiçbir şey görmezsin.

---

## 3. Text Nedir?

**Text**, ekrana yazı yazmak içindir. React Native'de yazıları doğrudan yazamazsın, mutlaka Text içine koymalısın.

### Yanlış Kullanım ❌

```tsx
<View>
  Merhaba Dünya
</View>
```
Bu ÇALIŞMAZ! Hata verir. Yazı mutlaka Text içinde olmalı.

### Doğru Kullanım ✅

```tsx
<View>
  <Text>Merhaba Dünya</Text>
</View>
```

---

## 4. İlk Gerçek Örnek

`app/index.tsx` dosyasını aç ve şunu yaz:

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View>
      <Text>Birinci satır</Text>
      <Text>İkinci satır</Text>
      <Text>Üçüncü satır</Text>
    </View>
  );
}
```

### Satır Satır Açıklama

```tsx
import { View, Text } from 'react-native';
```
`react-native` paketinden iki şey alıyoruz: View ve Text. Virgülle ayırarak birden fazla şey alabiliriz.

```tsx
export default function Index() {
```
`Index` adında bir fonksiyon oluşturuyoruz. `export default` bu fonksiyonu dışarıya açıyor, yani başka dosyalar bunu kullanabilir.

```tsx
return (
```
Fonksiyon bir şey döndürüyor (return). Parantez içindeki ekrana çizilecek.

```tsx
<View>
```
Bir kutu başlattık.

```tsx
<Text>Birinci satır</Text>
```
Kutu içine "Birinci satır" yazısı koyduk.

```tsx
</View>
```
Kutuyu kapattık.

```tsx
);
}
```
Fonksiyonu bitirdik.

---

## 5. View İçinde View

View'lar iç içe konulabilir. Kutu içinde kutu gibi.

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View>
      <Text>Dış kutunun yazısı</Text>
      
      <View>
        <Text>İç kutunun yazısı</Text>
      </View>
    </View>
  );
}
```

### Bu Neden Önemli?

İleride düzeni (layout) ayarlarken View'ları gruplamak gerekecek. Mesela:
- Bir View: Üst menü
- Bir View: Ana içerik
- Bir View: Alt menü

---

## 6. Birden Fazla Text

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View>
      <Text>Ad: Fatih</Text>
      <Text>Meslek: Öğretmen</Text>
      <Text>Şehir: Antalya</Text>
    </View>
  );
}
```

Her Text yeni satırda görünür.

---

## 7. Boş Satır Ekleme

Yazılar arasına boşluk koymak istersen boş bir View koyabilirsin (ileride daha iyi yöntemler öğreneceksin):

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View>
      <Text>Başlık</Text>
      
      <View style={{ height: 20 }} />
      
      <Text>Alt yazı</Text>
    </View>
  );
}
```

`style={{ height: 20 }}` kısmını şimdilik sadece kopyala. Bir sonraki modülde stil vermeyi öğreneceksin.

---

## 8. Text İçinde Text

Text'ler iç içe olabilir. Bu, bir cümlenin bir kısmını farklı biçimlendirmek için kullanılır:

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View>
      <Text>
        Bu normal yazı, <Text style={{ fontWeight: 'bold' }}>bu kalın yazı</Text>, bu yine normal.
      </Text>
    </View>
  );
}
```

---

## 9. Emoji Kullanımı

Text içinde emoji kullanabilirsin:

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View>
      <Text>🎉 Tebrikler!</Text>
      <Text>📱 Mobil uygulama yapıyorsun</Text>
      <Text>🚀 Harika gidiyorsun</Text>
    </View>
  );
}
```

Windows'ta emoji için: Windows tuşu + . (nokta)

---

## 10. Yorum Satırı

Kod içine açıklama yazmak istersen:

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View>
      {/* Bu bir yorum, ekranda görünmez */}
      <Text>Bu görünür</Text>
      
      {/* 
        Birden fazla satır
        yorum yazabilirsin
      */}
      <Text>Bu da görünür</Text>
    </View>
  );
}
```

JSX içinde yorum `{/* */}` şeklinde yazılır.

---

## 11. Pratik: Profil Kartı

Şimdi öğrendiklerinle basit bir profil kartı yap:

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View>
      {/* Profil Kartı */}
      <Text>👤 Profil</Text>
      <Text>━━━━━━━━━━━━━━</Text>
      <Text>Ad: Fatih</Text>
      <Text>Yaş: 35</Text>
      <Text>Meslek: IT Öğretmeni</Text>
      <Text>Şehir: Antalya</Text>
      <Text>━━━━━━━━━━━━━━</Text>
      <Text>🌐 coinsspor.com</Text>
    </View>
  );
}
```

Kaydet (Ctrl+S) ve telefonunda gör!

---

## 📝 Bu Modülde Öğrendiklerin

1. **View:** Görünmez kutu, içine şeyler koyarsın
2. **Text:** Yazı göstermek için, yazılar mutlaka Text içinde olmalı
3. **import:** Bileşenleri react-native'den almak
4. **export default function:** Sayfa fonksiyonu oluşturmak
5. **İç içe View:** Kutu içinde kutu yapabilirsin
6. **Yorum:** `{/* yorum */}` şeklinde yazılır

---

## ⚠️ Önemli Kurallar

1. Yazılar MUTLAKA Text içinde olmalı
2. Her açılan tag kapatılmalı: `<View>...</View>`
3. Büyük/küçük harf önemli: `View` doğru, `view` yanlış
4. import satırı dosyanın en üstünde olmalı

---

## ✅ Kontrol Listesi

- [ ] View'ın ne olduğunu anladım
- [ ] Text'in ne olduğunu anladım
- [ ] import satırını anlıyorum
- [ ] İç içe View yapabiliyorum
- [ ] Profil kartı örneğini yaptım

---

## 🔗 Sonraki Modül

[Modül 04: Stil Verme - Renk, Boyut, Kenarlık →](../04-style/README.md)
