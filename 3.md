# 📱 Modül 04: Stil Verme - Renk, Boyut, Kenarlık

Bu modülde View ve Text'e görünüm kazandıracaksın: renk, boyut, kenarlık, yuvarlak köşe...

---

## 1. Stil Nedir?

Şimdiye kadar yaptığın View ve Text'ler çok sade görünüyor. Stil (style) ile şunları yapabilirsin:

- Arka plan rengi ekleme
- Yazı rengini değiştirme
- Boyut belirleme (genişlik, yükseklik)
- Kenarlık ekleme
- Köşeleri yuvarlama
- Boşluk ekleme

---

## 2. Inline (Satır İçi) Stil

En basit stil verme yöntemi, doğrudan elemana yazmak:

```tsx
<View style={{ backgroundColor: 'red' }}>
```

### Dikkat Et: Çift Süslü Parantez

- Dış süslü parantez `{}` : "JavaScript kodu gelecek" demek
- İç süslü parantez `{}` : JavaScript'te obje tanımı

### İlk Örnek: Renkli Kutu

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View style={{ backgroundColor: 'lightblue', padding: 20 }}>
      <Text>Mavi kutunun içindeyim!</Text>
    </View>
  );
}
```

### Açıklama

- `backgroundColor: 'lightblue'` → Arka plan rengi açık mavi
- `padding: 20` → İç boşluk 20 piksel (içerik kenara yapışmasın)

---

## 3. Temel Stil Özellikleri

### Arka Plan Rengi

```tsx
style={{ backgroundColor: 'red' }}
style={{ backgroundColor: '#3498db' }}     // Hex renk kodu
style={{ backgroundColor: 'rgb(52,152,219)' }}  // RGB
```

### Yazı Rengi

```tsx
<Text style={{ color: 'white' }}>Beyaz yazı</Text>
<Text style={{ color: '#e74c3c' }}>Kırmızı yazı</Text>
```

### Yazı Boyutu

```tsx
<Text style={{ fontSize: 24 }}>Büyük yazı</Text>
<Text style={{ fontSize: 12 }}>Küçük yazı</Text>
```

### Yazı Kalınlığı

```tsx
<Text style={{ fontWeight: 'bold' }}>Kalın yazı</Text>
<Text style={{ fontWeight: 'normal' }}>Normal yazı</Text>
```

### Genişlik ve Yükseklik

```tsx
<View style={{ width: 200, height: 100, backgroundColor: 'orange' }}>
  <Text>200x100 kutu</Text>
</View>
```

---

## 4. Tam Örnek: Renkli Kutular

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View style={{ padding: 50 }}>
      
      {/* Kırmızı Kutu */}
      <View style={{ 
        backgroundColor: 'red', 
        width: 150, 
        height: 80,
        marginBottom: 10 
      }}>
        <Text style={{ color: 'white' }}>Kırmızı Kutu</Text>
      </View>

      {/* Yeşil Kutu */}
      <View style={{ 
        backgroundColor: 'green', 
        width: 150, 
        height: 80,
        marginBottom: 10 
      }}>
        <Text style={{ color: 'white' }}>Yeşil Kutu</Text>
      </View>

      {/* Mavi Kutu */}
      <View style={{ 
        backgroundColor: 'blue', 
        width: 150, 
        height: 80 
      }}>
        <Text style={{ color: 'white' }}>Mavi Kutu</Text>
      </View>

    </View>
  );
}
```

### Yeni Özellik: marginBottom

`marginBottom: 10` → Kutunun altına 10 piksel boşluk bırakır. Böylece kutular birbirine yapışmaz.

---

## 5. Margin ve Padding Farkı

Bu ikisi çok karıştırılır. Şöyle düşün:

**Padding (İç Boşluk):** Kutunun İÇİNDE, içerikle kenar arasındaki boşluk.

**Margin (Dış Boşluk):** Kutunun DIŞINDA, diğer kutularla arasındaki boşluk.

```
        margin (dış)
    ┌─────────────────┐
    │   padding (iç)  │
    │  ┌───────────┐  │
    │  │  İÇERİK   │  │
    │  └───────────┘  │
    │                 │
    └─────────────────┘
```

### Örnek

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View style={{ padding: 50 }}>
      
      {/* Padding örneği */}
      <View style={{ 
        backgroundColor: 'coral',
        padding: 30,           // İçerik kenardan 30 piksel uzakta
        marginBottom: 20 
      }}>
        <Text style={{ backgroundColor: 'white' }}>
          Padding: 30
        </Text>
      </View>

      {/* Margin örneği */}
      <View style={{ 
        backgroundColor: 'skyblue',
        margin: 30,            // Kutu diğerlerinden 30 piksel uzakta
      }}>
        <Text style={{ backgroundColor: 'white' }}>
          Margin: 30
        </Text>
      </View>

    </View>
  );
}
```

### Yön Belirtme

```tsx
padding: 20           // Dört taraf 20
paddingTop: 10        // Sadece üst
paddingBottom: 10     // Sadece alt
paddingLeft: 10       // Sadece sol
paddingRight: 10      // Sadece sağ
paddingHorizontal: 10 // Sol ve sağ
paddingVertical: 10   // Üst ve alt

// Aynısı margin için de geçerli:
margin: 20
marginTop: 10
marginBottom: 10
// vs...
```

---

## 6. Kenarlık (Border)

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View style={{ padding: 50 }}>
      
      {/* Kenarlıklı kutu */}
      <View style={{ 
        borderWidth: 2,           // Kenarlık kalınlığı
        borderColor: 'black',     // Kenarlık rengi
        padding: 20,
        marginBottom: 20
      }}>
        <Text>Kenarlıklı kutu</Text>
      </View>

      {/* Renkli kenarlık */}
      <View style={{ 
        borderWidth: 3,
        borderColor: '#e74c3c',   // Kırmızı
        padding: 20,
        marginBottom: 20
      }}>
        <Text>Kırmızı kenarlık</Text>
      </View>

      {/* Tek taraflı kenarlık */}
      <View style={{ 
        borderBottomWidth: 2,     // Sadece alt kenarlık
        borderBottomColor: 'blue',
        padding: 20
      }}>
        <Text>Sadece alt kenarlık</Text>
      </View>

    </View>
  );
}
```

---

## 7. Yuvarlak Köşe (Border Radius)

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View style={{ padding: 50 }}>
      
      {/* Hafif yuvarlak */}
      <View style={{ 
        backgroundColor: '#3498db',
        borderRadius: 10,         // Köşeler 10 piksel yuvarlanır
        padding: 20,
        marginBottom: 20
      }}>
        <Text style={{ color: 'white' }}>Yuvarlak köşe: 10</Text>
      </View>

      {/* Çok yuvarlak */}
      <View style={{ 
        backgroundColor: '#9b59b6',
        borderRadius: 30,
        padding: 20,
        marginBottom: 20
      }}>
        <Text style={{ color: 'white' }}>Yuvarlak köşe: 30</Text>
      </View>

      {/* Tam daire (genişlik = yükseklik ve radius yarısı) */}
      <View style={{ 
        backgroundColor: '#e74c3c',
        width: 100,
        height: 100,
        borderRadius: 50,         // 100/2 = 50
        justifyContent: 'center',
        alignItems: 'center'
      }}>
        <Text style={{ color: 'white' }}>Daire</Text>
      </View>

    </View>
  );
}
```

### Daire Yapmak İçin

Genişlik ve yükseklik eşit olmalı, borderRadius bunların yarısı olmalı:
- width: 100, height: 100, borderRadius: 50 → Daire
- width: 80, height: 80, borderRadius: 40 → Daire

---

## 8. StyleSheet Kullanımı

Şimdiye kadar stilleri doğrudan yazdık. Ama bu yöntem:
- Kodu karmaşık yapar
- Aynı stili tekrar tekrar yazmak gerekir
- Performansı düşürür

Çözüm: **StyleSheet**

```tsx
import { View, Text, StyleSheet } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      <View style={styles.card}>
        <Text style={styles.title}>Başlık</Text>
        <Text style={styles.description}>Açıklama yazısı</Text>
      </View>
    </View>
  );
}

// Stiller dosyanın altında tanımlanır
const styles = StyleSheet.create({
  container: {
    padding: 50,
  },
  card: {
    backgroundColor: 'white',
    padding: 20,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: '#ddd',
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 10,
  },
  description: {
    fontSize: 14,
    color: '#666',
  },
});
```

### Açıklama

```tsx
import { View, Text, StyleSheet } from 'react-native';
```
StyleSheet'i de import ettik.

```tsx
<View style={styles.container}>
```
`styles.container` diyerek aşağıda tanımladığımız stili kullanıyoruz. Tek süslü parantez yeterli.

```tsx
const styles = StyleSheet.create({
  container: {
    padding: 50,
  },
  // ... diğer stiller
});
```
`StyleSheet.create()` içinde stilleri tanımlıyoruz. Her stilin bir adı var (container, card, title...).

### Neden StyleSheet?

1. **Düzenli kod:** Stiller ayrı, JSX ayrı
2. **Tekrar kullanım:** Aynı stili birçok yerde kullan
3. **Performans:** React Native stilleri optimize eder
4. **Hata yakalama:** Yanlış yazılan özellik hata verir

---

## 9. Birden Fazla Stil Birleştirme

Bazen bir elemana birden fazla stil vermek istersin:

```tsx
import { View, Text, StyleSheet } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      
      {/* Tek stil */}
      <Text style={styles.text}>Normal yazı</Text>
      
      {/* İki stili birleştir (array ile) */}
      <Text style={[styles.text, styles.bold]}>Kalın yazı</Text>
      
      {/* Stil + inline */}
      <Text style={[styles.text, { color: 'red' }]}>Kırmızı yazı</Text>

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 50,
  },
  text: {
    fontSize: 18,
    marginBottom: 10,
  },
  bold: {
    fontWeight: 'bold',
  },
});
```

Köşeli parantez `[]` içinde stilleri virgülle ayırarak yazıyoruz. Sonraki stil öncekini ezer (override).

---

## 10. Pratik: Güzel Profil Kartı

Önceki modüldeki sade profil kartını şimdi güzelleştirelim:

```tsx
import { View, Text, StyleSheet } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      
      <View style={styles.card}>
        {/* Avatar (daire) */}
        <View style={styles.avatar}>
          <Text style={styles.avatarText}>F</Text>
        </View>

        {/* Bilgiler */}
        <Text style={styles.name}>Fatih</Text>
        <Text style={styles.title}>IT Öğretmeni</Text>

        {/* Ayırıcı çizgi */}
        <View style={styles.divider} />

        {/* Detaylar */}
        <Text style={styles.info}>📍 Antalya, Türkiye</Text>
        <Text style={styles.info}>💼 Blockchain Developer</Text>
        <Text style={styles.info}>🌐 coinsspor.com</Text>
      </View>

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f0f0f0',
    padding: 20,
    paddingTop: 60,
  },
  card: {
    backgroundColor: 'white',
    borderRadius: 15,
    padding: 30,
    alignItems: 'center',
    // Gölge (iOS)
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.1,
    shadowRadius: 8,
    // Gölge (Android)
    elevation: 5,
  },
  avatar: {
    width: 80,
    height: 80,
    borderRadius: 40,
    backgroundColor: '#3498db',
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 15,
  },
  avatarText: {
    color: 'white',
    fontSize: 32,
    fontWeight: 'bold',
  },
  name: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#333',
  },
  title: {
    fontSize: 16,
    color: '#666',
    marginTop: 5,
  },
  divider: {
    width: '100%',
    height: 1,
    backgroundColor: '#eee',
    marginVertical: 20,
  },
  info: {
    fontSize: 14,
    color: '#555',
    marginBottom: 8,
  },
});
```

### Yeni Özellikler

- `flex: 1` → Tüm ekranı kapla (sonraki modülde detaylı)
- `alignItems: 'center'` → İçerikleri ortala (sonraki modülde detaylı)
- `shadowColor, shadowOffset...` → iOS'ta gölge
- `elevation` → Android'de gölge

---

## 📝 Bu Modülde Öğrendiklerin

1. **Inline stil:** `style={{ }}`
2. **backgroundColor:** Arka plan rengi
3. **color:** Yazı rengi
4. **fontSize, fontWeight:** Yazı boyutu ve kalınlığı
5. **width, height:** Genişlik ve yükseklik
6. **padding:** İç boşluk
7. **margin:** Dış boşluk
8. **borderWidth, borderColor, borderRadius:** Kenarlık
9. **StyleSheet.create:** Düzenli stil tanımlama
10. **Stil birleştirme:** `[styles.a, styles.b]`

---

## ✅ Kontrol Listesi

- [ ] Inline stil yazabiliyorum
- [ ] Padding ve margin farkını anladım
- [ ] Kenarlık ekleyebiliyorum
- [ ] Köşeleri yuvarlayabiliyorum
- [ ] StyleSheet kullanabiliyorum
- [ ] Güzel profil kartını yaptım

---

## 🔗 Sonraki Modül

[Modül 05: Flexbox - Elemanları Düzenleme ve Hizalama →](../05-flexbox/README.md)
