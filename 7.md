# 📱 Modül 08: Navigasyon - Sayfalar Arası Geçiş

Bu modülde birden fazla sayfa oluşturmayı ve aralarında geçiş yapmayı öğreneceksin.

---

## 1. Navigasyon Nedir?

Bir uygulama tek sayfadan oluşmaz. Mesela:
- Ana sayfa
- Profil sayfası
- Ayarlar sayfası
- Ürün detay sayfası

Bu sayfalar arasında geçiş yapmak için **navigasyon** kullanılır.

---

## 2. Expo Router Nedir?

Expo Router, dosya tabanlı bir navigasyon sistemidir. 

**Dosya tabanlı** ne demek? `app` klasöründe oluşturduğun her dosya otomatik olarak bir sayfa olur:

```
app/
├── index.tsx      → Ana sayfa (/)
├── profil.tsx     → /profil
├── ayarlar.tsx    → /ayarlar
└── hakkinda.tsx   → /hakkinda
```

Yani URL yapısı = dosya yapısı.

---

## 3. Proje Yapısını Kontrol Et

`npx create-expo-app` ile oluşturduğun projede `app` klasörü var. İçinde:

```
app/
├── (tabs)/
│   ├── _layout.tsx
│   ├── index.tsx
│   └── explore.tsx
├── +html.tsx
├── +not-found.tsx
└── _layout.tsx
```

Bu biraz karmaşık. Basitleştirelim.

---

## 4. Basit Navigasyon Kurulumu

### Adım 1: app klasörünü temizle

`app` klasöründeki tüm dosyaları sil. Sonra sadece şu dosyaları oluştur:

### Adım 2: _layout.tsx oluştur

Bu dosya, uygulamanın ana düzenini belirler:

```tsx
// app/_layout.tsx
import { Stack } from 'expo-router';

export default function Layout() {
  return (
    <Stack>
      <Stack.Screen name="index" options={{ title: 'Ana Sayfa' }} />
      <Stack.Screen name="profil" options={{ title: 'Profil' }} />
      <Stack.Screen name="ayarlar" options={{ title: 'Ayarlar' }} />
    </Stack>
  );
}
```

### Adım 3: index.tsx oluştur (Ana Sayfa)

```tsx
// app/index.tsx
import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';
import { Link } from 'expo-router';

export default function AnaSayfa() {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>🏠 Ana Sayfa</Text>
      <Text style={styles.subtitle}>Hoş geldiniz!</Text>

      <Link href="/profil" asChild>
        <TouchableOpacity style={styles.button}>
          <Text style={styles.buttonText}>Profil Sayfasına Git</Text>
        </TouchableOpacity>
      </Link>

      <Link href="/ayarlar" asChild>
        <TouchableOpacity style={styles.button}>
          <Text style={styles.buttonText}>Ayarlar Sayfasına Git</Text>
        </TouchableOpacity>
      </Link>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
  },
  title: {
    fontSize: 32,
    fontWeight: 'bold',
    marginBottom: 10,
  },
  subtitle: {
    fontSize: 18,
    color: '#666',
    marginBottom: 40,
  },
  button: {
    backgroundColor: '#3498db',
    paddingVertical: 15,
    paddingHorizontal: 30,
    borderRadius: 10,
    marginBottom: 15,
    width: '100%',
    alignItems: 'center',
  },
  buttonText: {
    color: 'white',
    fontSize: 16,
    fontWeight: '600',
  },
});
```

### Adım 4: profil.tsx oluştur

```tsx
// app/profil.tsx
import { View, Text, StyleSheet } from 'react-native';

export default function Profil() {
  return (
    <View style={styles.container}>
      <View style={styles.avatar}>
        <Text style={styles.avatarText}>F</Text>
      </View>
      <Text style={styles.name}>Fatih</Text>
      <Text style={styles.info}>IT Öğretmeni</Text>
      <Text style={styles.info}>Antalya, Türkiye</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    paddingTop: 50,
    backgroundColor: '#f5f5f5',
  },
  avatar: {
    width: 100,
    height: 100,
    borderRadius: 50,
    backgroundColor: '#3498db',
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 20,
  },
  avatarText: {
    color: 'white',
    fontSize: 40,
    fontWeight: 'bold',
  },
  name: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 10,
  },
  info: {
    fontSize: 16,
    color: '#666',
    marginBottom: 5,
  },
});
```

### Adım 5: ayarlar.tsx oluştur

```tsx
// app/ayarlar.tsx
import { View, Text, Switch, StyleSheet } from 'react-native';
import { useState } from 'react';

export default function Ayarlar() {
  const [bildirimler, setBildirimler] = useState(true);
  const [karanlikMod, setKaranlikMod] = useState(false);

  return (
    <View style={styles.container}>
      <View style={styles.option}>
        <Text style={styles.optionText}>Bildirimleri Aç</Text>
        <Switch value={bildirimler} onValueChange={setBildirimler} />
      </View>

      <View style={styles.option}>
        <Text style={styles.optionText}>Karanlık Mod</Text>
        <Switch value={karanlikMod} onValueChange={setKaranlikMod} />
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    backgroundColor: '#f5f5f5',
  },
  option: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: 'white',
    padding: 15,
    borderRadius: 10,
    marginBottom: 10,
  },
  optionText: {
    fontSize: 16,
  },
});
```

---

## 5. Navigasyon Yapısını Anla

```
┌─────────────────────────────────────┐
│         ← Ana Sayfa                 │  ← Header (otomatik)
├─────────────────────────────────────┤
│                                     │
│         Sayfa İçeriği               │  ← Senin kodun
│                                     │
└─────────────────────────────────────┘
```

`Stack` navigasyonu kullandığımızda:
- Sayfalar üst üste yığılır (stack = yığın)
- Otomatik "geri" butonu eklenir
- Header otomatik oluşur

---

## 6. Link Bileşeni

Sayfalar arası geçiş için `Link` kullanılır:

```tsx
import { Link } from 'expo-router';

// Basit link
<Link href="/profil">
  <Text>Profile Git</Text>
</Link>

// Butonla link
<Link href="/profil" asChild>
  <TouchableOpacity style={styles.button}>
    <Text>Profile Git</Text>
  </TouchableOpacity>
</Link>
```

### asChild Ne Demek?

`asChild` olmadan Link kendi stilini uygular. `asChild` ile içindeki bileşenin stilini kullanır.

---

## 7. useRouter ile Programatik Navigasyon

Buton tıklama sonrası belirli bir koşula göre yönlendirme yapmak için:

```tsx
import { useRouter } from 'expo-router';

export default function GirisSayfasi() {
  const router = useRouter();

  const girisYap = () => {
    // Giriş kontrolü...
    
    // Başarılıysa ana sayfaya git
    router.push('/');
    
    // veya replace ile (geri dönüşü engeller)
    router.replace('/');
  };

  return (
    <TouchableOpacity onPress={girisYap}>
      <Text>Giriş Yap</Text>
    </TouchableOpacity>
  );
}
```

### router metodları

```tsx
router.push('/profil')     // Sayfaya git (geri gelebilir)
router.replace('/profil')  // Sayfayı değiştir (geri dönemez)
router.back()              // Bir önceki sayfaya dön
```

---

## 8. Dinamik Sayfa (Parametreli URL)

Mesela her ürünün kendi sayfası olsun: `/urun/1`, `/urun/2`, `/urun/3`...

### Adım 1: urun klasörü ve [id].tsx dosyası oluştur

```
app/
├── _layout.tsx
├── index.tsx
└── urun/
    └── [id].tsx      ← Köşeli parantez önemli!
```

### Adım 2: [id].tsx dosyası

```tsx
// app/urun/[id].tsx
import { View, Text, StyleSheet } from 'react-native';
import { useLocalSearchParams } from 'expo-router';

export default function UrunDetay() {
  const { id } = useLocalSearchParams();

  // Normalde bu veri API'den gelir
  const urunler = {
    '1': { isim: 'iPhone 15', fiyat: 50000 },
    '2': { isim: 'MacBook Pro', fiyat: 75000 },
    '3': { isim: 'AirPods', fiyat: 8000 },
  };

  const urun = urunler[id] || { isim: 'Bulunamadı', fiyat: 0 };

  return (
    <View style={styles.container}>
      <Text style={styles.id}>Ürün ID: {id}</Text>
      <Text style={styles.isim}>{urun.isim}</Text>
      <Text style={styles.fiyat}>₺{urun.fiyat.toLocaleString()}</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  id: {
    fontSize: 14,
    color: '#666',
    marginBottom: 10,
  },
  isim: {
    fontSize: 28,
    fontWeight: 'bold',
    marginBottom: 10,
  },
  fiyat: {
    fontSize: 24,
    color: '#27ae60',
  },
});
```

### Adım 3: Ana sayfadan link ver

```tsx
// app/index.tsx içinde
<Link href="/urun/1" asChild>
  <TouchableOpacity style={styles.button}>
    <Text style={styles.buttonText}>iPhone 15 Detay</Text>
  </TouchableOpacity>
</Link>

<Link href="/urun/2" asChild>
  <TouchableOpacity style={styles.button}>
    <Text style={styles.buttonText}>MacBook Pro Detay</Text>
  </TouchableOpacity>
</Link>
```

### useLocalSearchParams

```tsx
const { id } = useLocalSearchParams();
```
URL'deki parametreyi alır. `/urun/5` için `id` değeri `"5"` olur.

---

## 9. Header Özelleştirme

```tsx
// app/_layout.tsx
import { Stack } from 'expo-router';

export default function Layout() {
  return (
    <Stack
      screenOptions={{
        headerStyle: {
          backgroundColor: '#3498db',
        },
        headerTintColor: 'white',
        headerTitleStyle: {
          fontWeight: 'bold',
        },
      }}
    >
      <Stack.Screen 
        name="index" 
        options={{ 
          title: 'Ana Sayfa',
          headerShown: true,  // Header göster/gizle
        }} 
      />
      <Stack.Screen 
        name="profil" 
        options={{ title: 'Profilim' }} 
      />
    </Stack>
  );
}
```

### screenOptions

Tüm sayfalara uygulanacak ortak ayarlar:
- `headerStyle`: Header arka plan stili
- `headerTintColor`: Header yazı/ikon rengi
- `headerTitleStyle`: Başlık yazı stili

### options

Her sayfaya özel ayarlar:
- `title`: Header başlığı
- `headerShown`: Header gösterilsin mi?

---

## 10. Tab Navigasyonu (Alt Menü)

Instagram, Twitter gibi alt menülü yapı:

### Klasör yapısı

```
app/
├── _layout.tsx
└── (tabs)/              ← Parantezli klasör
    ├── _layout.tsx      ← Tab ayarları
    ├── index.tsx        ← Ana tab
    ├── arama.tsx        ← Arama tab
    └── profil.tsx       ← Profil tab
```

### (tabs)/_layout.tsx

```tsx
// app/(tabs)/_layout.tsx
import { Tabs } from 'expo-router';

export default function TabLayout() {
  return (
    <Tabs
      screenOptions={{
        tabBarActiveTintColor: '#3498db',
        tabBarInactiveTintColor: '#999',
      }}
    >
      <Tabs.Screen
        name="index"
        options={{
          title: 'Ana Sayfa',
          tabBarIcon: () => <Text>🏠</Text>,
        }}
      />
      <Tabs.Screen
        name="arama"
        options={{
          title: 'Arama',
          tabBarIcon: () => <Text>🔍</Text>,
        }}
      />
      <Tabs.Screen
        name="profil"
        options={{
          title: 'Profil',
          tabBarIcon: () => <Text>👤</Text>,
        }}
      />
    </Tabs>
  );
}
```

### Ana _layout.tsx

```tsx
// app/_layout.tsx
import { Stack } from 'expo-router';

export default function RootLayout() {
  return (
    <Stack>
      <Stack.Screen name="(tabs)" options={{ headerShown: false }} />
    </Stack>
  );
}
```

---

## 📝 Bu Modülde Öğrendiklerin

| Kavram | Açıklama |
|--------|----------|
| `_layout.tsx` | Sayfa düzenini belirler |
| `Stack` | Üst üste yığılan sayfalar |
| `Tabs` | Alt menülü sayfalar |
| `Link` | Sayfa linki |
| `useRouter` | Programatik yönlendirme |
| `[param].tsx` | Dinamik sayfa |
| `useLocalSearchParams` | URL parametresini al |

### Dosya → URL Eşleşmesi

```
app/index.tsx       → /
app/profil.tsx      → /profil
app/urun/[id].tsx   → /urun/123
app/(tabs)/         → Tab grubu
```

---

## ✅ Kontrol Listesi

- [ ] Stack navigasyonu kurabiliyorum
- [ ] Link ile sayfa geçişi yapabiliyorum
- [ ] useRouter kullanabiliyorum
- [ ] Dinamik sayfa oluşturabiliyorum
- [ ] Header'ı özelleştirebiliyorum
- [ ] Tab navigasyonu yapabiliyorum

---

## 🎉 Tebrikler!

React Native Expo temellerini tamamladın! Artık:
- Ekrana içerik koyabilirsin
- Stil verebilirsin
- Flexbox ile düzenleyebilirsin
- Kullanıcıdan veri alabilirsin
- Liste gösterebilirsin
- Sayfalar arası geçiş yapabilirsin

### Sonraki Adımlar

1. Öğrendiklerini birleştirerek bir proje yap
2. API'lerden veri çekmeyi öğren (fetch)
3. Kamera, konum gibi cihaz özelliklerini kullan
4. Uygulamanı mağazalara yükle

---

## 🔗 Faydalı Kaynaklar

- [Expo Dokümantasyonu](https://docs.expo.dev/)
- [React Native Dokümantasyonu](https://reactnative.dev/)
- [Expo Router Dokümantasyonu](https://docs.expo.dev/router/introduction/)

---

**İyi kodlamalar! 🚀**
